spring:
  application:
    name: productsAPI
  datasource:
    url: jdbc:h2:mem:products
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
      path: /h2-console